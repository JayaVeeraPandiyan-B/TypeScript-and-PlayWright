2 Statements irukku

  * Conditional
  * Looping

Conditional Statement: Oru group of statement ah condition potu use panna, iru epovum Ture or False ah tha decide pannum.. intha condition true ah iruntha block of code run aagum, false ah iruntha skip panni next statement or block of code ku pogum

Java la irukura if - else tha

* "if" false na "else if" ithuvum false na continues "else if" ella condition um false na last ah vera vazhi illama "else" nadula "else if" ethana true na angaye execution mudinjidum.
</> Markdown
```ts
let age : number = 20;

if (age >= 18)   {
    console.log("You're eligible to vote!!");
```

* "if, else if, else" inga la nama Condition kudupom but "switch = expression" kudupom, because condition epovum true or false tha return pannum but switch la case kudupom inga true or false illama enna vena return pannum
</> Markdown
```ts
if (age > 18 )   {
    console.log("You're eligible to vote!!")
} else {console.log("You're Minor!!")}

// **********

let num:number = 9

if (num%2 >= 1)   {
    console.log(`${num} is Odd number`)
} else (`${num} is Even number`)

// *********

Mark >= 90 Grade A
Mark >= 75 Grade B
Mark >= 60 Grade C
else Grade D


let marks : number = 52

if (marks >= 90 && marks <=100)   {
    console.log("Grade A")
} else if (marks >= 75 && marks <=89) {
    console.log("Grade B")
} else if (marks >= 60 && marks <= 74) {
    console.log("Grade C")
} else    {
    console.log("Grade D")
}

// **********

let browser : string = "Edge"

if (browser === "Chrome")   {console.log("Browser is Chrome")}
if (browser === "Safari")   {console.log("Browser is Safari")}
if (browser === "Edge")   {console.log("Browser is Edge")}
else {console.log(`Browser is ${browser}`)}

```

* Switch-case = Oru variable/value oda different cases match aagudha nu check panni corresponding block execute pannum.
</> Markdown
```ts
let day : number = 6

switch (day){
    case 1 : console.log("Today is Sunday"); break;
    case 2 : console.log("Today is Monday"); break;
    case 3 : console.log("Today is Tuesday"); break;
    case 4 : console.log("Today is Wednesday"); break;
    case 5 : console.log("Today is Thursday"); break;
    case 6 : console.log("Today is Friday"); break;
    case 7 : console.log("Today is Thursday"); break;
    default: (console.log("Invalid Day"))
```

* break = oru statement ah stop pannum.

* continue = oru statement ah skip pannum.

Key Points

  • if → Executes code if condition is true. 
  • if-else → Executes one block for true, another for false. 
  • if-else-if → Checks multiple conditions sequentially. 
  • switch-case → Compares a value against multiple cases for efficiency. 
