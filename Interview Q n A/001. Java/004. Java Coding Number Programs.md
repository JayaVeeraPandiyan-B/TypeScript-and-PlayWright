
# JAVA NUMBER PROGRAMS – INTERVIEW ANSWERS

---

# 33. Swap two numbers.

## Method 1 – Using Third Variable

### Program

```java id="w8k1x0"
public class SwapNumbers {
    public static void main(String[] args) {

        int a = 10;
        int b = 20;

        int temp = a;
        a = b;
        b = temp;

        System.out.println("a = " + a);
        System.out.println("b = " + b);
    }
}
```

---

## Method 2 – Without Third Variable

### Program

```java id="pv8h3m"
public class SwapWithoutTemp {
    public static void main(String[] args) {

        int a = 10;
        int b = 20;

        a = a + b;
        b = a - b;
        a = a - b;

        System.out.println(a);
        System.out.println(b);
    }
}
```

---

# 34. Factorial of a number.

### Explanation

Factorial of 5:
5! = 5 \times 4 \times 3 \times 2 \times 1 = 120

### Program

```java id="tv0vsm"
public class Factorial {
    public static void main(String[] args) {

        int num = 5;
        int fact = 1;

        for(int i=1; i<=num; i++) {
            fact = fact * i;
        }

        System.out.println("Factorial = " + fact);
    }
}
```

---

# 35. Fibonacci series.

### Explanation

Fibonacci series:
0, 1, 1, 2, 3, 5, 8, 13, \dots

Each number is sum of previous two numbers.

### Program

```java id="4wh6u0"
public class Fibonacci {
    public static void main(String[] args) {

        int a = 0;
        int b = 1;

        System.out.print(a + " " + b + " ");

        for(int i=1; i<=8; i++) {

            int c = a + b;

            System.out.print(c + " ");

            a = b;
            b = c;
        }
    }
}
```

---

# 36. Prime number check.

### Explanation

Prime number has only 2 factors:

* 1
* Itself

Example:
2, 3, 5, 7

### Program

```java id="mqz8y6"
public class PrimeNumber {
    public static void main(String[] args) {

        int num = 7;
        boolean prime = true;

        for(int i=2; i<num; i++) {

            if(num % i == 0) {
                prime = false;
                break;
            }
        }

        if(prime) {
            System.out.println("Prime Number");
        } else {
            System.out.println("Not Prime");
        }
    }
}
```

---

# 37. Armstrong number check.

### Explanation

Armstrong number means:
Sum of cube of digits equals original number.

Example:
153 = 1^3 + 5^3 + 3^3 = 153

### Program

```java id="s31u5s"
public class Armstrong {
    public static void main(String[] args) {

        int num = 153;
        int original = num;
        int sum = 0;

        while(num > 0) {

            int rem = num % 10;

            sum = sum + (rem * rem * rem);

            num = num / 10;
        }

        if(original == sum) {
            System.out.println("Armstrong Number");
        } else {
            System.out.println("Not Armstrong Number");
        }
    }
}
```

---

# 38. Perfect number check.

### Explanation

Perfect number means:
Sum of factors equals original number.

Example:
6 = 1 + 2 + 3

### Program

```java id="k3dhhz"
public class PerfectNumber {
    public static void main(String[] args) {

        int num = 6;
        int sum = 0;

        for(int i=1; i<num; i++) {

            if(num % i == 0) {
                sum = sum + i;
            }
        }

        if(sum == num) {
            System.out.println("Perfect Number");
        } else {
            System.out.println("Not Perfect Number");
        }
    }
}
```

---

# 39. Leap year validation.

### Explanation

Leap year conditions:

* Divisible by 4
* Not divisible by 100
  OR
* Divisible by 400

### Program

```java id="dykzdh"
public class LeapYear {
    public static void main(String[] args) {

        int year = 2024;

        if((year % 4 == 0 && year % 100 != 0)
                || (year % 400 == 0)) {

            System.out.println("Leap Year");
        }
        else {
            System.out.println("Not Leap Year");
        }
    }
}
```

---

# 40. Sum of digits.

### Example

For number:

```java id="w9r7tb"
1234
```

Sum:
1 + 2 + 3 + 4 = 10

### Program

```java id="j4q9v5"
public class SumOfDigits {
    public static void main(String[] args) {

        int num = 1234;
        int sum = 0;

        while(num > 0) {

            int rem = num % 10;

            sum = sum + rem;

            num = num / 10;
        }

        System.out.println(sum);
    }
}
```

---

# 41. Count digits in a number.

### Program

```java id="yr4r6t"
public class CountDigits {
    public static void main(String[] args) {

        int num = 12345;
        int count = 0;

        while(num > 0) {

            num = num / 10;

            count++;
        }

        System.out.println("Digit Count: " + count);
    }
}
```

---

# 42. GCD of two numbers.

### Explanation

GCD = Greatest Common Divisor

Example:
\gcd(12,18)=6

### Program

```java id="4sjhh0"
public class GCD {
    public static void main(String[] args) {

        int a = 12;
        int b = 18;

        int gcd = 1;

        for(int i=1; i<=a && i<=b; i++) {

            if(a % i == 0 && b % i == 0) {
                gcd = i;
            }
        }

        System.out.println("GCD = " + gcd);
    }
}
```

---

# 43. Convert decimal to binary.

### Example

10_{10} = 1010_2

### Program

```java id="vqf07y"
public class DecimalToBinary {
    public static void main(String[] args) {

        int num = 10;

        String binary = "";

        while(num > 0) {

            int rem = num % 2;

            binary = rem + binary;

            num = num / 2;
        }

        System.out.println(binary);
    }
}
```

---

# 44. Sum of first N natural numbers.

### Formula

\frac{n(n+1)}{2}

### Program

```java id="jx2hcf"
public class SumNaturalNumbers {
    public static void main(String[] args) {

        int n = 5;

        int sum = n * (n + 1) / 2;

        System.out.println("Sum = " + sum);
    }
}
```

---

# 45. Generate random numbers.

### Program

```java id="5j4wt0"
import java.util.Random;

public class RandomNumbers {
    public static void main(String[] args) {

        Random random = new Random();

        int number = random.nextInt(100);

        System.out.println("Random Number: " + number);
    }
}
```
