
# ARRAY PROGRAMS – INTERVIEW ANSWERS

---

# 18. Find the largest and second largest number in an array.

### Program

```java id="q7g5sy"
public class LargestSecondLargest {
    public static void main(String[] args) {

        int[] arr = {10, 50, 30, 90, 70};

        int largest = arr[0];
        int secondLargest = arr[0];

        for(int i=0; i<arr.length; i++) {

            if(arr[i] > largest) {
                secondLargest = largest;
                largest = arr[i];
            }
            else if(arr[i] > secondLargest && arr[i] != largest) {
                secondLargest = arr[i];
            }
        }

        System.out.println("Largest: " + largest);
        System.out.println("Second Largest: " + secondLargest);
    }
}
```

---

# 19. Find the smallest number in an array.

### Program

```java id="c8p0zh"
public class SmallestNumber {
    public static void main(String[] args) {

        int[] arr = {40, 10, 90, 5, 60};

        int smallest = arr[0];

        for(int i=1; i<arr.length; i++) {

            if(arr[i] < smallest) {
                smallest = arr[i];
            }
        }

        System.out.println("Smallest Number: " + smallest);
    }
}
```

---

# 20. Remove duplicate elements from an array or ArrayList.

### Using ArrayList

```java id="hr85vq"
import java.util.ArrayList;

public class RemoveDuplicates {
    public static void main(String[] args) {

        ArrayList<Integer> list = new ArrayList<>();

        list.add(10);
        list.add(20);
        list.add(10);
        list.add(30);

        ArrayList<Integer> unique = new ArrayList<>();

        for(Integer num : list) {

            if(!unique.contains(num)) {
                unique.add(num);
            }
        }

        System.out.println(unique);
    }
}
```

---

# 21. Rotate an array by K positions.

### Example

Input:

```java id="p7z5n0"
1 2 3 4 5
```

Rotate by 2 positions:

```java id="ojg4gq"
4 5 1 2 3
```

### Program

```java id="w0cf2z"
public class RotateArray {
    public static void main(String[] args) {

        int[] arr = {1,2,3,4,5};
        int k = 2;

        for(int i=0; i<k; i++) {

            int last = arr[arr.length-1];

            for(int j=arr.length-1; j>0; j--) {
                arr[j] = arr[j-1];
            }

            arr[0] = last;
        }

        for(int num : arr) {
            System.out.print(num + " ");
        }
    }
}
```

---

# 22. Reverse an array.

### Program

```java id="q0r4t4"
public class ReverseArray {
    public static void main(String[] args) {

        int[] arr = {10,20,30,40};

        for(int i=arr.length-1; i>=0; i--) {
            System.out.print(arr[i] + " ");
        }
    }
}
```

---

# 23. Check whether an array is sorted.

### Program

```java id="ajyq0r"
public class SortedArray {
    public static void main(String[] args) {

        int[] arr = {10,20,30,40};

        boolean sorted = true;

        for(int i=0; i<arr.length-1; i++) {

            if(arr[i] > arr[i+1]) {
                sorted = false;
                break;
            }
        }

        if(sorted) {
            System.out.println("Array is Sorted");
        } else {
            System.out.println("Array is Not Sorted");
        }
    }
}
```

---

# 24. Find missing number in a sequence.

### Example

```java id="8w4xt9"
1 2 3 5
```

Missing number:

```java id="3ryp4e"
4
```

### Program

```java id="rf78y1"
public class MissingNumber {
    public static void main(String[] args) {

        int[] arr = {1,2,3,5};

        int n = 5;

        int expectedSum = n * (n + 1) / 2;

        int actualSum = 0;

        for(int num : arr) {
            actualSum += num;
        }

        System.out.println("Missing Number: " + (expectedSum - actualSum));
    }
}
```

---

# 25. Find common elements in two arrays.

### Program

```java id="y0yajh"
public class CommonElements {
    public static void main(String[] args) {

        int[] arr1 = {1,2,3,4};
        int[] arr2 = {3,4,5,6};

        for(int i=0; i<arr1.length; i++) {

            for(int j=0; j<arr2.length; j++) {

                if(arr1[i] == arr2[j]) {
                    System.out.println(arr1[i]);
                }
            }
        }
    }
}
```

---

# 26. Move all zeroes to the beginning/end of an array.

## Move Zeroes to End

### Program

```java id="dq9fhw"
public class MoveZeroes {
    public static void main(String[] args) {

        int[] arr = {1,0,3,0,5,0,7};

        int index = 0;

        for(int i=0; i<arr.length; i++) {

            if(arr[i] != 0) {
                arr[index] = arr[i];
                index++;
            }
        }

        while(index < arr.length) {
            arr[index] = 0;
            index++;
        }

        for(int num : arr) {
            System.out.print(num + " ");
        }
    }
}
```

---

# 27. Print even and odd indexed elements.

### Program

```java id="8q4tx6"
public class EvenOddIndex {
    public static void main(String[] args) {

        int[] arr = {10,20,30,40,50};

        System.out.println("Even Index Elements:");

        for(int i=0; i<arr.length; i=i+2) {
            System.out.println(arr[i]);
        }

        System.out.println("Odd Index Elements:");

        for(int i=1; i<arr.length; i=i+2) {
            System.out.println(arr[i]);
        }
    }
}
```

---

# 28. Find pairs whose sum equals a target.

### Example

Target:

```java id="l5r4bt"
10
```

### Program

```java id="3w6f5f"
public class PairSum {
    public static void main(String[] args) {

        int[] arr = {1,2,3,7,8,9};

        int target = 10;

        for(int i=0; i<arr.length; i++) {

            for(int j=i+1; j<arr.length; j++) {

                if(arr[i] + arr[j] == target) {
                    System.out.println(arr[i] + " + " + arr[j]);
                }
            }
        }
    }
}
```

---

# 29. Merge two sorted arrays.

### Program

```java id="d4y5pt"
public class MergeArrays {
    public static void main(String[] args) {

        int[] arr1 = {1,3,5};
        int[] arr2 = {2,4,6};

        int[] merged = new int[arr1.length + arr2.length];

        int index = 0;

        for(int num : arr1) {
            merged[index++] = num;
        }

        for(int num : arr2) {
            merged[index++] = num;
        }

        for(int num : merged) {
            System.out.print(num + " ");
        }
    }
}
```

---

# 30. Print duplicate elements from an array.

### Program

```java id="xhf0ux"
public class DuplicateElements {
    public static void main(String[] args) {

        int[] arr = {1,2,3,2,4,5,1};

        for(int i=0; i<arr.length; i++) {

            for(int j=i+1; j<arr.length; j++) {

                if(arr[i] == arr[j]) {
                    System.out.println(arr[i]);
                }
            }
        }
    }
}
```

---

# 31. Print elements appearing more than once.

### Program

```java id="yqq9m6"
public class RepeatedElements {
    public static void main(String[] args) {

        int[] arr = {10,20,10,30,20,40};

        for(int i=0; i<arr.length; i++) {

            int count = 1;

            for(int j=i+1; j<arr.length; j++) {

                if(arr[i] == arr[j]) {
                    count++;
                }
            }

            if(count > 1) {
                System.out.println(arr[i]);
            }
        }
    }
}
```

---

# 32. Diagonal traversal in a 2D array.

### Program

```java id="n7km12"
public class DiagonalTraversal {
    public static void main(String[] args) {

        int[][] arr = {
                {1,2,3},
                {4,5,6},
                {7,8,9}
        };

        System.out.println("Primary Diagonal:");

        for(int i=0; i<arr.length; i++) {
            System.out.println(arr[i][i]);
        }

        System.out.println("Secondary Diagonal:");

        for(int i=0; i<arr.length; i++) {
            System.out.println(arr[i][arr.length-1-i]);
        }
    }
}
```
