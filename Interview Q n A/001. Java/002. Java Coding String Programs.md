Here are interview-style answers with Java coding examples for the questions from your uploaded file 

# JAVA CODING QUESTIONS – INTERVIEW ANSWERS

# STRING PROGRAMS

---

# 1. Reverse a string without using built-in reverse methods.

### Explanation

Convert string into character array and iterate from last to first.

### Program

```java
public class ReverseString {
    public static void main(String[] args) {
        String str = "Hello";
        String rev = "";

        for(int i = str.length() - 1; i >= 0; i--) {
            rev = rev + str.charAt(i);
        }

        System.out.println("Reversed String: " + rev);
    }
}
```

### Output

```java
olleH
```

---

# 2. Check whether a string is palindrome.

### Explanation

A palindrome reads same forward and backward.

Example:

* madam
* level

### Program

```java
public class Palindrome {
    public static void main(String[] args) {
        String str = "madam";
        String rev = "";

        for(int i = str.length()-1; i>=0; i--) {
            rev = rev + str.charAt(i);
        }

        if(str.equals(rev)) {
            System.out.println("Palindrome");
        } else {
            System.out.println("Not Palindrome");
        }
    }
}
```

---

# 3. Find duplicate characters in a string.

### Program

```java
public class DuplicateCharacters {
    public static void main(String[] args) {
        String str = "programming";

        for(int i=0; i<str.length(); i++) {
            for(int j=i+1; j<str.length(); j++) {

                if(str.charAt(i) == str.charAt(j)) {
                    System.out.println(str.charAt(i));
                    break;
                }
            }
        }
    }
}
```

---

# 4. Remove duplicate characters from a string.

### Program

```java
public class RemoveDuplicates {
    public static void main(String[] args) {

        String str = "programming";
        String result = "";

        for(int i=0; i<str.length(); i++) {

            char ch = str.charAt(i);

            if(result.indexOf(ch) == -1) {
                result = result + ch;
            }
        }

        System.out.println(result);
    }
}
```

### Output

```java
progamin
```

---

# 5. Count occurrences/frequency of each character in a string.

### Program

```java
public class CharacterFrequency {
    public static void main(String[] args) {

        String str = "hello";

        for(char ch='a'; ch<='z'; ch++) {

            int count = 0;

            for(int i=0; i<str.length(); i++) {

                if(ch == str.charAt(i)) {
                    count++;
                }
            }

            if(count > 0) {
                System.out.println(ch + " : " + count);
            }
        }
    }
}
```

---

# 6. Find the first non-repeated character in a string.

### Program

```java
public class FirstNonRepeated {
    public static void main(String[] args) {

        String str = "swiss";

        for(int i=0; i<str.length(); i++) {

            boolean unique = true;

            for(int j=0; j<str.length(); j++) {

                if(i != j && str.charAt(i) == str.charAt(j)) {
                    unique = false;
                    break;
                }
            }

            if(unique) {
                System.out.println("First Non-Repeated Character: " + str.charAt(i));
                break;
            }
        }
    }
}
```

---

# 7. Reverse words in a sentence.

### Input

```java
I love Java
```

### Output

```java
Java love I
```

### Program

```java
public class ReverseWords {
    public static void main(String[] args) {

        String str = "I love Java";

        String[] words = str.split(" ");

        for(int i=words.length-1; i>=0; i--) {
            System.out.print(words[i] + " ");
        }
    }
}
```

---

# 8. Remove all white spaces from a string.

### Program

```java
public class RemoveSpaces {
    public static void main(String[] args) {

        String str = "Java is easy";

        str = str.replaceAll("\\s", "");

        System.out.println(str);
    }
}
```

### Output

```java
Javaiseasy
```

---

# 9. Check whether two strings are anagrams.

### Explanation

Anagrams contain same characters in different order.

Example:

* listen
* silent

### Program

```java
import java.util.Arrays;

public class Anagram {
    public static void main(String[] args) {

        String str1 = "listen";
        String str2 = "silent";

        char[] arr1 = str1.toCharArray();
        char[] arr2 = str2.toCharArray();

        Arrays.sort(arr1);
        Arrays.sort(arr2);

        if(Arrays.equals(arr1, arr2)) {
            System.out.println("Anagram");
        } else {
            System.out.println("Not Anagram");
        }
    }
}
```

---

# 10. Extract digits from an alphanumeric string.

### Input

```java
abc123xyz45
```

### Output

```java
12345
```

### Program

```java
public class ExtractDigits {
    public static void main(String[] args) {

        String str = "abc123xyz45";
        String digits = "";

        for(int i=0; i<str.length(); i++) {

            char ch = str.charAt(i);

            if(Character.isDigit(ch)) {
                digits = digits + ch;
            }
        }

        System.out.println(digits);
    }
}
```

---

# 11. Convert uppercase letters to lowercase and vice versa.

### Program

```java
public class ToggleCase {
    public static void main(String[] args) {

        String str = "JaVa";
        String result = "";

        for(int i=0; i<str.length(); i++) {

            char ch = str.charAt(i);

            if(Character.isUpperCase(ch)) {
                result = result + Character.toLowerCase(ch);
            }
            else {
                result = result + Character.toUpperCase(ch);
            }
        }

        System.out.println(result);
    }
}
```

### Output

```java
jAvA
```

---

# 12. Find maximum occurring character in a string.

### Program

```java
public class MaxOccurringChar {
    public static void main(String[] args) {

        String str = "success";
        int maxCount = 0;
        char maxChar = ' ';

        for(int i=0; i<str.length(); i++) {

            int count = 0;

            for(int j=0; j<str.length(); j++) {

                if(str.charAt(i) == str.charAt(j)) {
                    count++;
                }
            }

            if(count > maxCount) {
                maxCount = count;
                maxChar = str.charAt(i);
            }
        }

        System.out.println(maxChar);
    }
}
```

---

# 13. Reduce multiple spaces between words to a single space.

### Program

```java
public class SingleSpace {
    public static void main(String[] args) {

        String str = "Java    is     easy";

        str = str.replaceAll("\\s+", " ");

        System.out.println(str);
    }
}
```

---

# 14. Check whether one string contains another string.

### Program

```java
public class ContainsString {
    public static void main(String[] args) {

        String str1 = "Java Programming";
        String str2 = "Program";

        if(str1.contains(str2)) {
            System.out.println("Contains");
        } else {
            System.out.println("Not Contains");
        }
    }
}
```

---

# 15. Find string length without using length().

### Program

```java
public class StringLength {
    public static void main(String[] args) {

        String str = "Hello";
        int count = 0;

        for(char ch : str.toCharArray()) {
            count++;
        }

        System.out.println(count);
    }
}
```

---

# 16. Convert String to Integer.

### Program

```java
public class StringToInteger {
    public static void main(String[] args) {

        String str = "123";

        int num = Integer.parseInt(str);

        System.out.println(num);
    }
}
```

---

# 17. Print characters multiple times based on numeric values in the string.

### Input

```java
a3b2c4
```

### Output

```java
aaabbcccc
```

### Program

```java
public class RepeatCharacters {
    public static void main(String[] args) {

        String str = "a3b2c4";

        for(int i=0; i<str.length(); i=i+2) {

            char ch = str.charAt(i);

            int count = Character.getNumericValue(str.charAt(i+1));

            for(int j=1; j<=count; j++) {
                System.out.print(ch);
            }
        }
    }
}
```
