* Object - it is a group of variables and methods in programming language concept, another words objects is a collection of key and value phase

    * TypeScript la Object na key–value pair la data store panna use pannura structure.
          * Simple ah sonna: Object = properties (keys) + values

    * Properties means variables
    * Behaviour means methods

* There are different ways to create an object in JS/TS,

    * Using object type - directly define the value for variable (JS/TS)
    * Inline type object - we also define the datatype of the keys (TS)
    * Using the aliases (JS/TS)
    * Using the Classes (JS - ES16/TS)
 
* **Using Object type**

The TypeScript 'Object' type represent all the values which are non primitive

Basic Example:
</> TypeScript
```ts
let person = {
    name : "Jaya Veera Pandiyan B",
    age : 34,
    city : "Chennai"
};

console.log(person);
console.log("Employee name:",person.name);
console.log("Employee age:",person.age);
console.log("Employee city:",person.city);
```
<img width="595" height="214" alt="image" src="https://github.com/user-attachments/assets/37546c2b-7add-4b8f-bc30-fb8b68fb89c3" />


* **Inline type object**

Enna type nu define panni kooda object create panalam

</> TypeScript
```ts
let person : {name:string; age:number; city:string} = {
    name : "Jaya Veera Pandiyan B",
    age : 34,
    city : "Chennai"
};

console.log(person);
console.log("Employee name:",person.name);
console.log("Employee age:",person.age);
console.log("Employee city:",person.city);
```
<img width="593" height="191" alt="image" src="https://github.com/user-attachments/assets/d4c03e39-efee-4378-813d-fa8d710f3698" />

Ithula enna problem na structure ovvoru vaatiyum create pananum example obj 1 la name, age, gender obj 2 la same key another variable na again vera structure create pananum code usablity athigam ithu alias nu onnu irukku

* **function in Object**

Oru object ulla function kooda pass panalam

Object la oru function irukku antha function ulla object oda variable vachi value venum na "this.objectVariableName" koota varum

Example
</> TypeScript
```ts
let person = {
    name : "Jaya Veera Pandiyan B",
    age : 34,
    wlcm : "Hello da!!",
    greet : function () : string {
        return `${this.wlcm}`;
    }
};

console.log(person.name);
console.log(person.age);
console.log(person.greet()); // Easy
```
<img width="513" height="178" alt="image" src="https://github.com/user-attachments/assets/9d525206-773b-4661-b059-b6f8fdc4feb8" />

</> TypeScript
```ts
let person = {
    name : "Jaya Veera Pandiyan B",
    age : 34,
    wlcm : "Hello da!!",
    greet : function () : string {
        return `${this.wlcm}`;
    }
};
console.log(person["name"], person["age"]);
console.log(person["greet"]()); // Kastam
```

Ipdi easy ah um koopdalam kastamavum koopdalam

* **Nested Object**

Nested ah value ah kooda store pani koopdalam

Example
</> TypeScript
```ts
let person = {
    name : "Jaya Veera Pandiyan B",

    age :    {
        personAge : 34
    }
};

console.log(person.name);
console.log(person.age.personAge);
```
<img width="502" height="163" alt="image" src="https://github.com/user-attachments/assets/dfe6c884-94f0-4fc8-8275-97bb96ee0b51" />

* **Modifying the existing object value**

Example
</> TypeScript
```ts
let person = {
    name : "Jaya Veera Pandiyan B",
    role : "Engineer"
};

console.log("Original value is:", person.role);
person.role = "Manager";
console.log("After modification value is:", person.role);
```
<img width="494" height="149" alt="image" src="https://github.com/user-attachments/assets/fa97c268-2f3d-4d64-b48d-74b6d83c584a" />

* **Alias Object type**

Type ah thani object ah declare pannitu intha type object ah ethana vaati venalum ethana object ah venalum call pani use panikalam

Example
</> TypeScript
```ts
type person = {
    name : string;
    age : number;
};

let userPerson1 : person = {
    name:"Jaya Veera Pandiyan B",
    age:34
};

let userPerson2 : person = {
    name : "Bala Murugan B",
    age : 34
};

console.log("First person name is :",userPerson1.name);
console.log("First person age is :",userPerson1.age);
console.log("Second person name is :",userPerson2.name);
console.log("Second person age is :",userPerson2.age);
```
<img width="494" height="190" alt="image" src="https://github.com/user-attachments/assets/eeff5fdc-bbd7-4a90-82eb-4a4b551ca229" />


* **Intersection Types** - we can combine multiple types.. multiple types create panitu ellathayum combine panni use panalam

Example
</> TypeScript
```ts
type person = {         // Individual
    name : string,
    age : number
}

type contact = {        // Individual
    email : string,
    mobile : number
}

type profile = person & contact &    {      // combination of other individuals in one type
    getContactInfo:()=> string;
}

let profileInfo1 : profile = {              // Implementation
    name : "Jaya Veera Pandiyan B",
    age : 34,
    email : "jayaveerapandiyanb@gmail.com",
    mobile : 9042631080,
    getContactInfo:function() {
        return `${this.name} can be reach via ${this.email} or ${this.mobile}`
    }
}

console.log(profileInfo1.getContactInfo()); // calling inside function 
```
<img width="770" height="138" alt="image" src="https://github.com/user-attachments/assets/9b23ca9b-74dc-4e21-830e-4cf02f8593f1" />
