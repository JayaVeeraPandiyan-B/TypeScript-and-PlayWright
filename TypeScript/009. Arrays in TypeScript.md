* TypeScript Arrays - An array in TypeScript is a special type of variable that can store multiple values. These values can be of the same type or a combination of different types.

* Declaring an Array - Arrays in TypeScript can be declared in two main ways:
    * Using square brackets ([]) (array literal syntax).
    * Using Array<Type> (generic array syntax).

Approach 1: Array Literal 
</> Markdown
```ts
let names: string[] = [];  // Declaration of an empty string array 
// Initializing values into the array 
names[0] = "john";  
names[1] = "smith"; 
names[2] = "peter"; 
names[3] = "scott";
```
</> Markdown
```ts
Approach 2: Generic Array Syntax
// Alternative way to initialize an array 
let names2: string[] = ["john", "smith", "peter", "scott"];  
let empNames: Array<string> = ["john", "smith", "peter", "scott"]; // Array of strings 
let empIds: Array<number> = [101, 102, 103, 104]; // Array of numbers 
let data: Array<string | number> = ["john", "smith", 101, 102]; // Union type (string or number) 
let data2: Array<any> = [1, "john", true, null]; // Array allowing multiple data types
```

* Accessing Array Elements
    * Access by index (indexing starts from 0).
    * Printing arrays using console.log().
</> Markdown
```ts
console.log(names);    // Output: ['john', 'smith', 'peter', 'scott'] 
console.log(names[1]); // Output: smith 
console.log(names[4]); // Output: undefined (index out of bounds)
```

* Iterating Over an Array 
There are multiple ways to loop through an array:

</> Markdown
```ts
//Using a for loop 
for (let i = 0; i < empNames.length; i++) { 
    console.log(empNames[i]); 
} 

//Using for...in loop (indexes) 
for (let i in empIds) { 
    console.log(empIds[i]); // 'i' represents index 
} 

//Using for...of loop (values) 
for (let element of data) { 
    console.log(element); // 'element' represents actual array values 
}
```

* Passing an Array to a Function
</> Markdown
```ts
Arrays can be passed to functions for processing. 
Example: Searching for an Element in an Array 
function search(ele: number, arr: number[]): boolean { 
    for (let i = 0; i < arr.length; i++) { 
        if (arr[i] === ele) { 
            return true; // Element found 
        } 
    } 
    return false; // Element not found 
} 
let arr: number[] = [10, 20, 30, 40, 50]; 
console.log(search(20, arr));  // Output: true 
console.log(search(100, arr)); // Output: false 
```
* Function Returning an Array - A function can take an array as input and return a modified array.

Example: Capitalizing Words in an Array 
</> Markdown
```ts
function capitalizeWords(arr: string[]): string[] { 
    let result: string[] = []; 
    for (let i = 0; i < arr.length; i++) { 
        result[i] = arr[i].toUpperCase(); 
    } 
    return result; 
} 
 
let words: string[] = ["hello", "world", "typescript"]; 
console.log(capitalizeWords(words)); // Output: ["HELLO", "WORLD", "TYPESCRIPT"]
```

* Key Takeaways 
    * Arrays store multiple values and can hold elements of the same or mixed types. 
    * Two ways to declare arrays: array literals ([]) and generic syntax (Array<Type>). 
    * Array indexing starts from 0. 
    * Elements can be accessed, modified, and iterated using loops (for, for...in, for...of). 
    * Arrays can be passed to functions for processing. 
    * Functions can return modified arrays.
 
* Tuple – A Special Type of Array 
    * A tuple is a fixed-length array where each element has a specific type.
    * It helps in storing multiple fields of different data types together.
</> Markdown
```ts
let person: [string, number] = ["Alice", 25]; 
console.log(person[0]); // Output: Alice 
console.log(person[1]); // Output: 25
```

* Rough
</> Markdown
```ts
//Approach 1 using Litrel

let names : string[] = []; // Declaration

// Initialization/ Assigning values

names [0] = "Jaya";
names [1] = "Veera";
names [2] = "Pandiyan";
names [3] = "Babu";

console.log(names);
```
</> Markdown
```ts
// Using Declaration and Initialization/ Assigning values

let names : string[] = ["Jaya", "Veera", "Pandiyan", "Babu"];

console.log(names);

//Approach 2 using the Generic Array<T> type

let names : Array<string> = ["Jaya", "Veera", "Pandiyan", "Babu"];

let nums : Array<number> = [2000, 105, 128];

console.log(names);
console.log(nums);
```
</> Markdown
```ts
//Multiple Daya types ah store pananum na

let datas : Array<string | number> = ["Jaya", "Veera", "Pandiyan", "B", 2000105128]

console.log(datas);

let mixedTypes : Array<any> = ["Jaya", "Veera", "Pandiyan", "B", 2000105128, true];

console.log(mixedTypes);
```
</> Markdown
```ts
//Example 1: Iteration over an array using a traditional for loop

let mixedTypes : Array<any> = ["Jaya", "Veera", "Pandiyan", "B", 2000105128, true];

console.log("Size of the Array:", mixedTypes.length);

//Type: 1

console.log("Type: 1");

for (let i:any = 0; i <= mixedTypes.length-1; i++)  {
        console.log(mixedTypes[i]);     
}

//Type: 2 

console.log("Type: 2");

for (let i:any = 0; i < mixedTypes.length; i++)  {
        console.log(mixedTypes[i]);     
}
```

</> Markdown
```ts
//Example 2: Iteration using the "for ... in" loop (indexes) - Just for print in loop - Iterate Key

console.log();
console.log("for ... in");
console.log();

let mixedTypes : Array<any> = ["Jaya", "Veera", "Pandiyan", "B", 2000105128, true];

for (let i in mixedTypes)  {
        console.log(mixedTypes[i]);
}
```
</> Markdown
```ts
//Example 3: Iteration using the "for ... of" loop (indexes) - Iterate Value

console.log("for ... of");
console.log();

let mixedTypes : Array<any> = ["Jaya", "Veera", "Pandiyan", "B", 2000105128, true];

for (let value of mixedTypes)  {
        console.log(value);
}
```
</> Markdown
```ts
// Example 4: Passing an Array to the function using traditional for loop

console.log("Passing an Array to the function");
console.log();

function search(ele:number, arr:number[]) : boolean  {

    for (let i = 0; i <= arr.length-1; i++)  {
            if (arr[i] === ele)   {
                    return true;
            }
    } 
    return false;
}

let arr : number[] = [10, 20, 30, 40, 50];

console.log(search(30, arr));
console.log(search(60, arr));
```
</> Markdown
```ts
// Example 5: Passing an Array to the function using "for...in" loop

function search(ele:number, arr:number[]):boolean {
    for(let i in arr)   {
            if(arr[i] === ele)    {
                return true;
            }
    }
    return false;
}

let arr = [30, 50, 70, 90];

console.log(search(30, arr));
console.log(search(100, arr));
```
</> Markdown
```ts
// Example 6: A function takes an array and returns an array

function stringDemo(words:string[]) : string[] {

    let res : string[] = [];

    for(let i:number = 0; i <= words.length-1; i++)   {
           res[i] = words[i].toLocaleUpperCase();     
    };
            return res;
}

let word : string[] = ["jaya", "VeerA", "pANDIyan", "b"];
console.log(stringDemo(word));
```
</> Markdown
```ts
// Example 7: Tuple with 2 value of string, number

let values : [string, number] = ["Jaya",30];

console.log(values);
console.log(values[0]);
console.log(values[1]);
```
</> Markdown
```ts
// Example 8: Tuple with multiple values

let user : [string, number, boolean, string, boolean, number] = ["Jaya", 30, true, "Veera", true, 31];

console.log("Tuple length:",user.length);
console.log(user[2]);

for (let i : number = 0; i <= user.length-1; i++)  {
        console.log(user[i]);
}


for (let i in user)  {
        console.log(user[i]);
}


for (let value of user)  {
        console.log(value);
}
```
</> Markdown
```ts
// Example 9: Tuple Array (Array of tuples)

let tupleTest : [string, number][] = [["Jaya",10], ["Veera",20], ["Pandiyan",30]];

let tp = tupleTest[2];

console.log("Type 1:",tp[0]);

// or

console.log("Type 2:",tupleTest[2][0]);
```
